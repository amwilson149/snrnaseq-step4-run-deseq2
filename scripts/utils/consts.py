import numpy as np

# Max int32 value (for producing
# valid random seeds with our
# random number generator)
INT32_MAX = np.iinfo(np.int32).max

# Number of numeric places that should
# be occupied by MHBB patient IDs
NUMERIC_PT_ID_WIDTH = 6
